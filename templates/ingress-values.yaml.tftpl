artifactory:
  ingress:
    enabled: true
    hosts:
      - ${hostname}
    annotations:
      kubernetes.io/ingress.class: "gce"
      kubernetes.io/ingress.global-static-ip-name: "${static_ip_name}"
      ingress.gcp.kubernetes.io/pre-shared-cert: "${ssl_cert_name}"
      networking.gke.io/v1beta1.FrontendConfig: "artifactory-frontend-config"
    tls: []

  nginx:
    service:
      type: NodePort